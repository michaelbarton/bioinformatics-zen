---
---
%head
  %meta{:charset => 'utf-8'}
  %title
    = site_title

  -# Specify author of the site
  %link{ :href => @site.config[:author][:url], :rel => "author" }

  -# Import google fonts specified in config.yml
  = google_fonts

  -# Style sheets
  = stylesheet relative_path_to("/stylesheets/screen.css")
  = stylesheet relative_path_to("/stylesheets/print.css"), 'print'

  = favicon

  = scripts

  -# HTML5 modernizer
  / [if IE]
    %script{:src => "http://html5shiv.googlecode.com/svn/trunk/html5.js"}
