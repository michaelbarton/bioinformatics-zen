---
---
.page-header
  %a{:href => relative_path_to(@item) }
    %h1
      = "#{@item[:title]}"
  %small
    = "Posted: " + Time.parse(item[:created_at]).strftime('%b %d, %Y')
  - if item[:revised]
    %small
      = "Significantly revised: " + Time.parse(item[:revised]).strftime('%b %d, %Y')
