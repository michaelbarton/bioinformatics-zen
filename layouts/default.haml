---
---
!!!
%html{ :lang => 'en' }
  = render "_header"
  %body
    = render "_title"
    %article
      %section
        %header
          %a{:href => @site.config[:site][:url] + @item.path }
            %h2
              = "#{@item[:title]}"
        = yield
-# Uncomment these lines after filling in the urls
-#     = render "_disqus"
-#   = render "_analytics"
